version: '3.8'

services:
  web:
    image: ravi0706/flask-app:latest  
    container_name: flask_app
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
    volumes:
      - flask_db:/app/instance
    restart: unless-stopped

volumes:
  flask_db:
